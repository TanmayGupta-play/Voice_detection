# Whisper for ASR
git+https://github.com/openai/whisper.git

# Torch backend (required by Whisper)
torch

# Audio preprocessing (via ffmpeg)
ffmpeg-python

# For evaluating accuracy (optional but recommended)
jiwer

# Utilities (optional, used in some versions)
numpy
